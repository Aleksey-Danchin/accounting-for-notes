# Copy to .env and adjust locally. Do not put real secrets in git.

TZ=Europe/Moscow
NODE_ENV=development

POSTGRES_USER=afn
POSTGRES_PASSWORD=afn
POSTGRES_DB=afn
POSTGRES_PORT=5432

BACKEND_PORT=3000
FRONTEND_PORT=5173
PRISMA_STUDIO_PORT=5555

# Frontend (Vite reads from repo root via envDir)
VITE_API_BASE_URL=/api
VITE_API_CONCURRENCY=5
