model Tag {
    id String @id @default(cuid())

    name        String
    color       String
    description String

    actions    Action[]
    requisites RequisiteTag[]

    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
    deletedAt DateTime?
}
